{
  "name": "hub-digital-igreja-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "vercel-build": "npm run build:frontend"
  }
}
